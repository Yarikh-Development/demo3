<Page x:Class="demo.TextAdminInterface"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:demo"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="500"
      Title="TextAdminInterface">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DefinedStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="#EEEDEF">
        <Grid.RowDefinitions>
            <!--行-->
            <RowDefinition Height="40"/>
            <!--<RowDefinition Height="20"/>-->
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0 0 0 8">
            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                <RadioButton Click="ZPL_Click" Style="{StaticResource BackGroundRadioButton}" Margin="0 0 20 0" Content="ZPL" FontSize="15" IsChecked="True" Width="100"/>
                <RadioButton Click="SGD_Click" Style="{StaticResource BackGroundRadioButton}" Content="SGD" FontSize="15" Width="100"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="ZPL" Grid.Row="1" HorizontalAlignment="Center" Background="White" VerticalAlignment="Top" Visibility="Visible">


            <ListView x:Name="listView" Height="auto" VerticalAlignment="Top" HorizontalAlignment="Left" Width="auto" BorderThickness="0,0,0,1" BorderBrush="#FFC8CCD4">
                <ListView.View>
                    <!--设置列标签并将列成员与一个变量名称相绑定，以便后台链接修改-->
                    <GridView>
                        <GridViewColumn Header="指令" Width="100" DisplayMemberBinding="{Binding Path=Direct}"/>
                        <GridViewColumn Header="说明" Width="100" DisplayMemberBinding="{Binding Path=Explain}"/>
                        <GridViewColumn Header="操作" Width="100" DisplayMemberBinding="{Binding Path=Operate}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton Width="30" Content="预览" Click="Preview_Click" />
                                        <RadioButton Width="30" Content="复制" Click="Copy_Click" />
                                        <RadioButton Width="30" Content="删除" Click="Copy_Click" />
                                    </StackPanel>

                                </DataTemplate>

                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>









        </Grid>

        <Grid x:Name="SGD" Grid.Row="1" HorizontalAlignment="Center" Background="White" VerticalAlignment="Top" Visibility="Hidden">
            <ListView x:Name="listView2" Height="auto" VerticalAlignment="Top" HorizontalAlignment="Left" Width="auto" BorderThickness="0,0,0,1" BorderBrush="#FFC8CCD4" >
                <ListView.View>
                    <!--设置列标签并将列成员与一个变量名称相绑定，以便后台链接修改-->
                    <GridView>
                        <GridViewColumn Header="指令1" Width="100" DisplayMemberBinding="{Binding Path=Direct}"/>
                        <GridViewColumn Header="说明" Width="100" DisplayMemberBinding="{Binding Path=Explain}"/>
                        
                        <GridViewColumn Header="操作" Width="100" DisplayMemberBinding="{Binding Path=Operate}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <RadioButton Width="30" Content="预览" Click="Preview_Click" />
                                        <RadioButton Width="30" Content="复制" Click="Copy_Click" />
                                        <RadioButton Width="30" Content="删除" Click="Copy_Click" />
                                    </StackPanel>
                                    
                                </DataTemplate>
                                
                            </GridViewColumn.CellTemplate>
                            
                        </GridViewColumn>

                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

    </Grid>
</Page>
